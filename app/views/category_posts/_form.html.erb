<%= form_with(model: category_post, local: true) do |form| %>
  <% if category_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category_post.errors.count, "error") %> prohibited this category_post from being saved:</h2>

      <ul>
      <% category_post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.text_field :category_id %>
  </div>

  <div class="field">
    <%= form.label :post_id %>
    <%= form.text_field :post_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
