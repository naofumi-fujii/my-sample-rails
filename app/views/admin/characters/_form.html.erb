<%= form_with(model: admin_character, local: true) do |form| %>
  <% if admin_character.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_character.errors.count, "error") %> prohibited this admin_character from being saved:</h2>

      <ul>
      <% admin_character.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
